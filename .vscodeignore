# Development-only files
.vscode/**
src/**

# Build / config not needed at runtime
tsconfig.json

# Internal notes
PLAN.md

# (optional) If you don't want source maps published, uncomment:
# out/**/*.map
